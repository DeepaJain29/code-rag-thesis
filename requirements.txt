# --- core RAG stack (System A) ---
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
faiss-cpu>=1.8.0            # swap for a GPU faiss build if your CUDA supports it
sentence-transformers>=3.0.0
transformers>=4.44.0
accelerate>=0.33.0
torch>=2.3.0                # install the CUDA build from pytorch.org for GPU runs
huggingface_hub>=0.24.0
datasets>=2.20.0

# --- utils ---
pyyaml>=6.0
numpy>=1.26
tqdm>=4.66
rapidfuzz>=3.9              # fast edit similarity; pure-python fallback exists

# --- optional ---
bitsandbytes>=0.43.0 ; platform_system == "Linux"   # only for load_in_4bit
